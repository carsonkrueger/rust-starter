[package]
name = "templates"
version = "0.1.0"
edition = "2024"

[dependencies]
once_cell = "1.21.3"
strum = { version = "0.27.2", features = ["derive"] }
templr = { version = "0.2.5", features = ["axum"] }
tw_merge = "0.1.7"
